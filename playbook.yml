---
- name: test vault
  gather_facts: false
  hosts: ET
  tasks:
    - name: dummy test
      debug:
        msg:
          - "{{ test_password }}"
          - "{{ jdbc_test_password }}"
          - "{{ api_test_password }}"
